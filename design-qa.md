# AI Stack 图谱与全站 Header 设计验收

验收日期：2026-07-15

验收基线：`codex/history-tags-header-fix`

目标：在保留 AI Stack 原有赛博终端视觉与全站导航的前提下，实现分层加载的动态社区知识图谱，并保证所有用户可见页面头部一致。

## 同视口视觉对照

参考图与最终实现均为 1625×968，状态均为“社区已展开 + 详情抽屉可见”。

| 参考图 | 最终实现 |
| --- | --- |
| ![参考社区图谱](docs/assets/design-qa/reference-community-1625.webp) | ![最终社区图谱](docs/assets/design-qa/final-community-1625.webp) |

实现保留了参考图的左侧工作台、外围社区场、中心琥珀热点、社区路径和右侧详情抽屉。以下差异为明确的产品约束，不属于视觉缺陷：

- 顶部使用全站唯一共享 Header，而不是参考图中的图谱专属 Header。
- 节点名称、文章数、连接强度与社区规模全部来自真实生成数据，不使用参考图中的演示数字。
- 社区展开限制为 24 个热点；本次实测为 22 节点、52 连线，以维持交互帧率和可读性。

## 响应式证据

| 1920×900 宽屏 | 390×844 移动端 |
| --- | --- |
| ![宽屏技术总览](docs/assets/design-qa/final-wide-1920.webp) | ![移动端图谱](docs/assets/design-qa/final-mobile-390.webp) |

- 宽屏 Header 高度 64px；图谱容器、Cytoscape Canvas 与页面可用列宽一致，无水平溢出。
- 2048px 视口测试时，应用内截图器受宿主 1961px 可视宽限制而截去 87px；1920px 复测完整，确认不是页面裁切。
- 移动端 Header 高度 64px，菜单与 RSS 触控区均为 44×44px；菜单可打开，Esc 可关闭；页面无横向滚动。

## 全站 Header 契约

对首页、文章列表、文章详情、标签总览、标签详情、分类总览、分类详情、图谱、场景详情、搜索、关于、归档与 404 共 13 个构建产物进行了浏览器测量：

- 每页只存在一个 `[data-site-header]`。
- 每页只加载一份 `style.css` 与一份 `tailwind.css`。
- Header 均为 64px，标题均为 16px/16px，说明均为 11px。
- 字体栈、品牌、遥测三行基线、导航顺序和子路径激活态一致。
- 404 的 CRT 闪烁只作用于正文，不再影响 Header。

## 交互与可靠性

- `03 节点邻域`：25 节点、24 连线，按钮可点击且真实切换到邻域模式。
- `02 标签社区`：首层 11 节点、10 连线；展开社区后 22 节点、52 连线并显示社区详情。
- 搜索 `api`：返回 10 条分层结果；ArrowDown + Enter 聚焦 `API` 并切换到邻域模式。
- 搜索、社区、邻域均未超过 100 节点/500 连线验收上限。
- 图谱、搜索与 Header 交互过程中浏览器 console error/warning：0。
- Python、Node、Hugo、Pagefind、图谱验证和静态产物测试全部通过。

## 缺陷结论

- P0：0
- P1：0
- P2：0
- P3：0（真实数据密度与参考演示数据不同，为预期差异）

final result: passed

---

# 趋势洞察增量设计验收

验收日期：2026-07-16

目标：在复用全站唯一 Header 与深蓝、青绿、少量琥珀视觉语言的前提下，为 `/trends/` 提供可解释、可下钻且不依赖持续动画的趋势工作台。

## 视觉证据与对照口径

- Source visual truth：`docs/assets/design-qa/reference-community-1625.webp`。该图作为工作台层级、深蓝底色、青绿信号与琥珀选中态的设计语言基线；趋势页是新增的信息产品，不把社区图谱的空间布局当作像素级模板。
- Implementation screenshot：`docs/assets/readme/trends-overview.webp`，1280×800，30 天窗口，Python 已选中，筛选、矩阵、真实锚点、详情与共享 Header 同屏。
- Mobile implementation screenshot：`docs/assets/design-qa/trends-mobile-390.webp`，390×844，30 天窗口，筛选已折叠，首条趋势在首屏可见。
- Full-view comparison：参考图与实现图已在同一次图像对照中打开；实现保留参考图的三栏情报工作台、冷色信号场、少量琥珀强调、真实统计与右侧下钻，差异来自“社区拓扑”与“定量趋势矩阵”的产品语义，不属于布局漂移。
- Focused region comparison：重点放大了共享 Header、矩阵标注/锚点、右侧评分与来源分布、移动端筛选/首条趋势。字体、间距、颜色、图像清晰度与文案均逐项复核；Canvas 为原生高 DPR 数据可视化，不包含替代图片、占位图或装饰性伪资产。

## 视觉与信息层级

- 桌面端采用“趋势筛选 / 信号矩阵与排名 / 证据下钻”三栏结构，与知识图谱页的工作台层级一致，但不复制图谱的装饰性节点。
- 矩阵横轴编码增长方向，纵轴编码证据数量，气泡大小编码来源覆盖；轨迹使用真实前序周期数据，未制造动画或伪实时效果。
- 宽屏标注得分最高的 8 个主题，紧凑工作台降为 6 个，短视口降为 4 个，并始终保留选中、悬停主题；文字进入左右注释通道，气泡碰撞偏移限制为 28px，并通过引线连接不偏移的真实定量锚点。
- 机器来源标识转换为稳定的用户可读名称，例如“博客与播客”“Hacker News”“掘金”“arXiv”。
- 初始详情栏使用明确空状态；用户选择主题后才加载对应主题分片，保持视觉安静并避免无意义首屏请求。

## 下钻与状态恢复

- 主题选择写入 URL；刷新、前进和后退均可恢复窗口、筛选、视图与主题详情。
- 证据文章在当前标签页打开，浏览器返回后恢复原趋势主题与筛选状态。
- “进入知识图谱聚焦”跳转到 `/scenarios/?mode=focus&node=<namespaced-id>`；图谱在引擎 ready 后只请求一次目标邻域，不预载错误核心节点。
- 当前窗口没有有效信号时明确提示，不借用其他窗口的评分；来源、场景、分类和证据文章均明确标注为 30 天档案。
- 数据没有 `external_url` 字段，页面不伪造外部原文链接，只展示来源标签与真实站内文章地址。

## 响应式与性能

- 390×844 紧凑视图自动切换为可访问列表，隐藏 Canvas 和视图切换器；筛选折叠为 44px 可操作摘要，首条趋势进入首屏且页面无水平溢出。
- 观察窗口、清除筛选与证据文章卡片的触控高度均不低于 44px。
- Canvas 仅在数据、筛选、尺寸或交互状态变化时重绘，不创建持续 `requestAnimationFrame` 循环。
- 列表与移动端视图不执行隐藏 Canvas 的布局或绘制。
- 主题详情按需加载，并使用 8 项 LRU 缓存与 `AbortController` 取消过期请求。

## 对照迭代历史

- Pass 1：发现 5 个 P2——短视口矩阵被截断、移动端先看到完整筛选而非结果、11px 弱文字对比度临界、详情关闭缺少焦点回归、碰撞避让让气泡偏离定量位置。修复后，1280×720 矩阵完整显示；移动端折叠筛选并露出 Python；弱文字透明度由 0.54 提升到 0.58；详情标题获得程序化焦点且关闭/Esc 返回来源；真实锚点保持不动并以引线表达最大 28px 的气泡偏移。
- Pass 2：发现浏览器前进/后退的 `popstate` 尚未复用焦点生命周期。修复后，后退到无详情状态时 Python 排名条目恢复为 `[active]`，前进到详情状态时 Python 标题恢复为 `[active]`；实测 URL 与视觉状态同步。
- Pass 3：独立只读复核未发现剩余 P0/P1/P2。字体与共享 Header、间距/首屏密度、颜色/对比度、Canvas 清晰度、中文文案、键盘状态和移动端触控目标均通过。

## 验证结果

- Node 趋势与图谱深链专项：17/17 tests passed；全量 Node：41/41 tests passed。
- Python 全量非 E2E：809 passed、1 skipped、84 subtests passed；最终趋势专项：60 passed。
- Hugo 0.153.4：9,199 pages，构建通过。
- Pagefind 1.5.2：1,938 pages、51,253 words、4 filters、1 sort，构建通过。
- 浏览器：文章跳转与返回恢复、图谱 focus 深链、筛选折叠、详情关闭/Esc、历史前进/后退、1280×720、1440×900 与 390×844 均通过；console error/warning 为 0。
- 数据：31 个静态分片通过身份、截止时间、引用、字节数与 SHA-256 交叉校验。
- P0：0；P1：0；P2：0；P3：0。

final result: passed
